<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xiaoshu.dao.GoodtypeMapper">
	<select id="findType" resultType="goodtype" parameterType="goodtype">
		select * from goodtype
	</select>
	<insert id="addType" parameterType="goodtype">
		insert into goodtype values(null,#{typename})
	</insert>
</mapper>